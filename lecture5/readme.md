Классы, перегрузка операторов

(на прошлой лекции)
2а конструктор
2б деструктор
2в конструктор копирования

3) Операторы ., -> и ключевое слово this
. - обращение к полю s.str_ (например)
-> p это указатель на объект класса (деревья Декартовы, указатели, связные списки)
(*p).f();
p -> f();


Пример:
for (int i = 5; i-->0; ...) это не --> а i-- (инкремент) + >

this - это указатель на тот объект, в котором мы сейчас

-------------------------
class String
{
public:
    String(const char* str, int size){ // конструктор
        this -> str = new char[size]; // выделение памяти

    }
    ~String() {delete[] this -> str;} // деструктор
private:
    char* str;
    int str;
};
-------------------------

всегда его писать не надо, только при необходимости



4) Оператор присвания

int main(){
    String s = String("abc", 3);
    String s1 = s;

    s = s1; 
    // Что произойдет? RunTimeError
    // Оператор присвания не определен + утечка памяти
}

Это метод, у нее должно что возвращаться

(Возвращаемый тип) (Название функции) (Принимаемые аргументы) (Тело функции)

String& operator = (const String& s) {
    if (&s == this)
        return *this // проверка на присваивание самому себе
    delete[] str;
    str = new char[s.size];
    for(...){...};
    return *this;
}

x = y = 5
x = y

int main(){
    String s = String("abc", 3);
    String s1 = s;

    s = s; 
    // Что произойдет? Старый массив потеряем, а новый скопируем сам в себя
    // Поэтому в операторе присваивания нужно проверить
    if (&s == this)
    return *this
}

The rule of 3 (Правило трех)
Если класса требует конструктора, деструктора или копирования хотя бы одного из них,
то значит надо писать все вместе

В C++ 11 вместо правила 3 есть правило 5

5) 

class C
{
    // Поля
    const int x = 3;
    int& y;
}

есть щас сделать C() - то будет ошибка, компилятор не генерирует конструктор по умолчанию, нужно самому писать его

Но я хочу свой конструктор от 
C(const int x, int& y)
{
    this -> x = x; // Ошибка потому что это константа
}

// В момент ){ конструктор уже проинициализирован полями по умолчанию, что делать?

5) Списки инициализации в констукторах
C(const int x, int& y):  x(x); y(y) {}

6) Вопрос для собеседования

String s = s1 + 'a'; // Компилятор пытается преобразование типов 

// Когда много типов и у них много разных конструкторов хочется, чтобы
неявные преобразования были запрещены

Ключевое слово explicit явный (implicit неявный)

explicit String(int size){...} // запрещено преобразование

Рекомендуется по возможности делать констукторы explicit


Константные методы

7) 

метод size() 

// getter setter 
// read only
int size() {return size_;}

const String s = "abs";
s.size(); // ошибка компиляции потому что size как будто size() имеет право 
менять поля объекта s, но он константный

Как исправить? Это жизненно
int size() const {return size_;} // сам метод является константым по отношению к
объекту от которого он вызывается

8) Ключевое слово mutable

mutable int x; // позволяет менять поле несмотря на то, что объект константный

9) Есть класс и в нем есть некоторые представления
И мы хотим определить метод в поток для cin

Ключевое слово friend
Оператор не является членом класса, но он нужен внутри класса

Я разрешаю ему трогать friend f(...);
Не надо создавать много друзей, много надо будет в памяти держать
надо относиться также, как и к static cast

10) 
Вернемся к конструкторам

конструктор от строки, числа и я хочу задать доп параметр capacity

Хочу написать конструктор, который повторяет старый код конструктора + еще что то 
дополнительное делает

String(int, const char*)

Появилось в C++11 - Делегирующие конструкторы
хочу написать String(const char*): String(int size, const char*)

11) Пример: у меня есть класс с объектами и полями и должно быть поле, которое
не принадлежит ни одному объекту static

Ключевое слово static

static int x;

12) Пример: у меня есть класс с полями  
Указатель на члены

хочу объявить на указать на поле
struct C{
    int a;
    char b;


}


int main(){
    int C::* p = C::a;
    С .* p = 5;


    еще можно сделать C ->* p 
    // :: - проход через область видимости
}


(Римская 4) Перегрузка опереторов

Арифметические операторы

BigInteger operator +(const BigInteger& x) const {
    BigInteger sum = *this;
    return sum += x;
}


BigInteger& operator +=(const BigInteger& x) const {
    return *this = *this + x; // Так нельзя писать!!! 
    // Каждое сложение 1 занимает линейное время    
}

x = x + 1; //
x += y;